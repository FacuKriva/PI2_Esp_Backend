server:
  port: 8082

spring:
  application:
    name: msvc-users-api

  # Conexion a DB MySQL
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/user_db?useUnicode=true&characterEncoding=utf8&useSSL=false&requireSSL=false&createDatabaseIfNotExist=true
    username: root
    password: root

  # JPA - Hibernate
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: false
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: true
  properties:
    hibernate:
      format_sql: false
  # Lectura del archivo sql
  sql:
    init:
      mode: always
      data-locations-classpath: import.sql

  mail:
    host: smtp.gmail.com
    port: 587
    username: 4nR34lvarez@gmail.com
    password: ttrvgkmasxihivbz
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
logging:
  level:
    org:
      hibernate:
        SQL: debug

  # Eureka
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka