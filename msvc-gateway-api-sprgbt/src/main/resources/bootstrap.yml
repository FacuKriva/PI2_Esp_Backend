spring:
  application:
    name: msvc-gateway-api-sprgbt

server:
  port: 8080

#Eureka
eureka:
  instance:
    instance-id: ${spring:application:name}:${spring:application:instance_id:${random:value}}
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka